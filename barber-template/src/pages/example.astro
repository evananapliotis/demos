---
/**
 * /example: the fictional Halden & Crane shop page, built from the same
 * components as every listing's page. The front page shows it live inside a
 * phone. Noindex, unlisted.
 */
import ShopPage from '../components/demo/ShopPage.astro';
import { mockSite } from '../lib/home.ts';
---

<ShopPage site={mockSite} footer={false} />
