---
/** /example/book: the fictional shop's booking page, so every link on /example resolves. */
import BookingPage from '../../components/demo/BookingPage.astro';
import { mockSite } from '../../lib/home.ts';
---

<BookingPage site={mockSite} />
