---
/**
 * Renders nothing. astro.config.mjs aliases a component to this when a build
 * flag turns its feature off, so the real component's markup, styles and
 * script stay out of the bundle instead of shipping unreferenced.
 */
---
