# Copy to .env (gitignored). The scripts look here first, then in the repo root.

# Google Places API (New) key, used by `npm run photos`. Enable "Places API (New)" on the key.
GOOGLE_PLACES_KEY=

# Pexels API key, used by `npm run images:fetch`.
PEXELS_API_KEY=
