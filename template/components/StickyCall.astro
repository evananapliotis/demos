---
import { client, telHref } from '../lib/client';
---
<div id="sticky-call" class="fixed inset-x-0 bottom-0 z-40 translate-y-full bg-ink/85 p-3 pb-[max(0.75rem,env(safe-area-inset-bottom))] backdrop-blur-sm transition-transform duration-300 ease-out md:hidden" aria-hidden="true">
  <a href={telHref} class="btn w-full" tabindex="-1">
    <span>Call {client.phone.display}</span>
  </a>
</div>
<script>
  const bar = document.getElementById('sticky-call');
  const hero = document.getElementById('top');
  const link = bar?.querySelector('a');
  if (bar && hero && link) {
    const show = (on: boolean) => {
      bar.classList.toggle('translate-y-full', !on);
      bar.setAttribute('aria-hidden', on ? 'false' : 'true');
      link.tabIndex = on ? 0 : -1;
    };
    if ('IntersectionObserver' in window) {
      new IntersectionObserver(([e]) => show(!e.isIntersecting), { threshold: 0.05 }).observe(hero);
    } else {
      show(true);
    }
  }
</script>
